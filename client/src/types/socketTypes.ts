// Re-export all shared types — single source of truth in /shared/types.ts
export type {
  GameStateEvent,
  PlayerInfo,
  GameAction,
  GuessEvent,
  GameBroadcast,
  HangGuySocketEvents,
} from "../../../shared/types";
